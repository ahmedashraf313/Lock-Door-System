#include "spiconfig.h"

//the proto types of the functions
void    masterInit      (void   );
uint8_t masterTransive  (uint8_t);
void    slaveInit       (void   );
uint8_t slaveTransive   (uint8_t); 

